import {modal} from "../shared/modal/modal";
import {accordion} from "../faqPage/accordion/accordion";
import {floatingButtons} from "../shared/floatingButtons/floatingButtons";

document.addEventListener("DOMContentLoaded", () => {
  modal()
  accordion()
  floatingButtons()
})
